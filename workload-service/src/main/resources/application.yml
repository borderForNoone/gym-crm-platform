spring:
  application:
    name: workload-service

server:
  port: 8082
  servlet:
    context-path: /workload-service

api:
  base-path: /api/v1

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:}
  instance:
    prefer-ip-address: true

jwt:
  secret: ${JWT_SECRET}