spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL;NON_KEYWORDS=value;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        query:
          startup_check: false
  liquibase:
    change-log: classpath:db/test-migration/master.xml
  sql:
    init:
      mode: never

app:
  api:
    base-path: /api/v1
  cors:
    allowed-origins: http://localhost:3000