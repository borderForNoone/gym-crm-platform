spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false
    properties:
      hibernate:
        type:
          preferred_boolean_jdbc_type: TINYINT

logging:
  level:
    root: WARN
    org.springframework: WARN
    com.gym.crm: INFO