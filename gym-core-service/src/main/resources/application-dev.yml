spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    url: /gia-api.yml
    path: /swagger-ui/index.html
  api-docs:
    enabled: true

logging:
  level:
    root: INFO
    org.springframework: INFO
    com.gym.crm: DEBUG